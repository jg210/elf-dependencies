
* Set up as ruby gem.

* Automated build for gem.

* Tests.

* Add example of output to README.


* Make repo public.

* Don't show backtrace just because provided with bad input.

* Publish gem.

* Allow dependencies to be found from LD_LIBRARY_PATH, not just command-line.

* More robust quoting of graphviz identifiers.

