* Set up as ruby gem.

* Automated build for gem.

* Tests.
  - --help option
  - test parsing of mocked objdump output
  - apply to everything under /usr/lib/?
  - quoting of graphviz identifiers.

* rubocop static analysis.

* Add relevant rbenv configuration from aws-experiments repo.

* Check that objdump is installed.

* Don't show backtrace just because provided with bad input.

* Publish gem.

* Allow dependencies to be found from LD_LIBRARY_PATH, not just command-line.

* Optionally fail if don't find all libraries.


